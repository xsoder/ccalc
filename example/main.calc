print('%', "Urmom")

struct Area {
    x,
    area
}

p = Area {
    x: 100,
    area : lambda self: { x = x * x }
}

function() = {
    x = p.area()
    print(x)
    print("The area is %", p.area())
    print(p.x)
}

function()
