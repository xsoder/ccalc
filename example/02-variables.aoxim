# += or any such variant is still not supported and will not be
math = 20
print('Interpolation %' , math)

math = math + 1
print(math)

math = "It can also be a string"

print("{math}")

const PI = double(22)/double(7)
print("{PI}")

PI = 200
print("{PI}")
