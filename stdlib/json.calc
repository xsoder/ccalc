import "file"
import "map"

struct Json {
    path,
    file,
    obj = map_new()
    parse(self) = {
        assert(self.path != None)
        self.file = File {
            path: self.path,
            self.file.open(path)
        }
        self.file.open(self.path)
    }
}

